
# def hyperbolic_pdf(x):
#     """ Hyperbolic radius density to sample points from """
#     return (np.exp((ndim - 1) * x) - np.exp((ndim - 1) * -x))/2

# def sample_hyperbolic(ndim, Rmin, Rmax):
#     """ 
#     Generate random points on an ndim-dimensional hyperbolic ball with a specified lower and upper bound on the radius 
#     :return an array with the first entry being the random radius and the second entry being the array of angles
#     """

#     # Pick a random radii within bounds using inverse transform sampling
#     random_radius = sample(hyperbolic_pdf, 1, lower_bd=Rmin, upper_bd=Rmax)

#     # Pick (ndim-1) uniform random angles
#     angles = np.random.uniform(0, 1, ndim-1)

#     return [random_radius, angles] 




# # return a distance matrix using the hyperbolic metric
# def sample_new(num_points, ndim=3):
#     # First obtains random points
#     random_points = np.random.uniform(0,1,(n_samples,ndim)) # may need to standardize with radius = 1

#     # compute norm and divide to make each point of radius 1
#     for row in random_points:
#         row_norm = np.linalg.norm(row)
#         row = row / row_norm

#     # create distance matrix
#     dist_matrix = np.zeros(shape=(num_points, num_points))

#     # fill in distance matrix with inter-point distances (diagonal is one, the rest)
#     for i in range(0, dist_matrix.shape[0]):
#         dist_matrix[i,i] = 1

#     for i in range(0, dist_matrix.shape[0]):
#         for j in range(i, dist_matrix.shape[0]):
#             dist_matrix[i,j] = compute_distance(random_points[i,:], random_points[j,:]) 
#             dist_matrix[j,i] = compute_distance(random_points[i,:], random_points[j,:]) 
    
#     return dist_matrix


# # computes the hyperbolic distance between two points 
# def compute_distance(p1, p2):
#     return 2 * np.arcsinh((np.linalg.norm(p1-p2))/(2*np.sqrt(p1[-1] * p2[-1])))